(ns alchemy.db)

(def default-items [
                    {:name "fire"} 
                    {:name "water"} 
                    {:name "earth"}
                    {:name "air"}])
(def default-db
  {:available default-items})
